#!/usr/bin/env python3
import subprocess
import sys

x = sys.argv[1].split()
timecode = x[0]
command = ' '.join(x[1:])
output_image_path = command.split()[-1]
subprocess.call(command, shell=True)
print(f"{timecode} {output_image_path}")
